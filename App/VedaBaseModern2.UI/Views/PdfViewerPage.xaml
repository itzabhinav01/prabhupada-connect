<Page
    x:Class="VedaBaseModern.UI.Views.PdfViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Navigation Bar -->
        <Grid Grid.Row="0" Padding="16,8" Background="{ThemeResource NavigationViewDefaultPaneBackground}"
              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Back Button & Book Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <Button x:Name="BackButton" Click="BackButton_Click" Style="{StaticResource TextBlockButtonStyle}" Padding="6" ToolTipService.ToolTip="Back to Library">
                    <FontIcon Glyph="&#xE72B;" FontSize="14" />
                </Button>
                <StackPanel Orientation="Vertical">
                    <TextBlock x:Name="BookTitleText" Text="PDF Document" Style="{StaticResource BodyStrongTextBlockStyle}" FontSize="15" />
                    <TextBlock x:Name="AuthorText" Text="Vaiṣṇava Ācārya" FontSize="12" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                </StackPanel>
            </StackPanel>

            <!-- Actions: Remove Book -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button x:Name="RemoveBookButton" Click="RemoveBookButton_Click" Style="{StaticResource TextBlockButtonStyle}" ToolTipService.ToolTip="Remove this PDF book from library">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="#FF6B6B" />
                        <TextBlock Text="Remove Book" FontSize="12" Foreground="#FF6B6B" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- PDF WebView2 Container -->
        <Grid Grid.Row="1">
            <controls:WebView2 x:Name="PdfWebView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
            <ProgressRing x:Name="LoadingRing" Width="48" Height="48" HorizontalAlignment="Center" VerticalAlignment="Center" IsActive="True" />
        </Grid>
    </Grid>
</Page>
