<Page
    x:Class="VedaBaseModern.UI.Views.WebBrowserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Browser Navigation Bar -->
        <Border Grid.Row="0" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="12,8">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Back Button -->
                <Button Grid.Column="0" x:Name="BackButton" Click="BackButton_Click" IsEnabled="False" ToolTipService.ToolTip="Back">
                    <FontIcon Glyph="&#xE72B;" FontSize="13"/>
                </Button>

                <!-- Forward Button -->
                <Button Grid.Column="1" x:Name="ForwardButton" Click="ForwardButton_Click" IsEnabled="False" ToolTipService.ToolTip="Forward">
                    <FontIcon Glyph="&#xE72A;" FontSize="13"/>
                </Button>

                <!-- Reload Button -->
                <Button Grid.Column="2" x:Name="RefreshButton" Click="RefreshButton_Click" ToolTipService.ToolTip="Reload">
                    <FontIcon Glyph="&#xE72C;" FontSize="13"/>
                </Button>

                <!-- Address Bar -->
                <TextBox Grid.Column="3" x:Name="UrlTextBox" KeyDown="UrlTextBox_KeyDown" PlaceholderText="Enter web address or search... (e.g. prabhupadavani.com)" VerticalAlignment="Center" />

                <!-- Loading Spinner -->
                <ProgressRing Grid.Column="4" x:Name="LoadingRing" Width="18" Height="18" IsActive="False" VerticalAlignment="Center" Margin="4,0" />

                <!-- Open in External Browser -->
                <Button Grid.Column="5" x:Name="ExternalButton" Click="ExternalButton_Click" ToolTipService.ToolTip="Open in external browser">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE8A7;" FontSize="13"/>
                        <TextBlock Text="External" FontSize="12"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- WebView2 Control -->
        <controls:WebView2 Grid.Row="1" x:Name="BrowserWebView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
    </Grid>
</Page>
